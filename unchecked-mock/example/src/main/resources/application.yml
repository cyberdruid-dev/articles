spring:
  datasource:
    url: jdbc:h2:mem:tickets;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate # schema ownership belongs to Flyway, not Hibernate
